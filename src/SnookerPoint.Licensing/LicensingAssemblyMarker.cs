namespace SnookerPoint.Licensing;

/// <summary>
/// Marker type identifying the Licensing assembly. This project will hold the
/// shared signed-certificate model and client-side (public-key only) verification,
/// reused by the Phase 9 server. No licensing services exist yet — they are
/// deliberately out of scope for Phase 0.
/// </summary>
public static class LicensingAssemblyMarker
{
}
