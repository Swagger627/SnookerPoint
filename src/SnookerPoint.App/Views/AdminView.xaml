<UserControl x:Class="SnookerPoint.App.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:SnookerPoint.App.Controls"
             Background="{DynamicResource Brush.Background}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Height="64" Background="{DynamicResource Brush.Surface}"
                BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <TextBlock Text="Database &amp; diagnostics" Style="{StaticResource Text.Heading}" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,8,0" />
                    <Button Content="Theme" Command="{Binding ToggleThemeCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,8,0" />
                    <Button Content="Home" Command="{Binding HomeCommand}" Style="{StaticResource Button.Secondary}" />
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="28" ctrl:SmoothScrollBehavior.Enabled="True">
            <StackPanel MaxWidth="1000" HorizontalAlignment="Left">
                <ctrl:FeedbackBanner Kind="{Binding Feedback.Kind}" Message="{Binding Feedback.Message}"
                                     DismissCommand="{Binding Feedback.DismissCommand}" Margin="0,0,0,14" />

                <Border Style="{StaticResource Card}" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Health" Foreground="{DynamicResource Brush.Text}" FontWeight="SemiBold" Margin="0,0,0,10" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Application version" Style="{StaticResource Text.Body}" Margin="0,3" />
                                <TextBlock Text="Schema version" Style="{StaticResource Text.Body}" Margin="0,3" />
                                <TextBlock Text="Database location" Style="{StaticResource Text.Body}" Margin="0,3" />
                                <TextBlock Text="Database size" Style="{StaticResource Text.Body}" Margin="0,3" />
                                <TextBlock Text="Available disk space" Style="{StaticResource Text.Body}" Margin="0,3" />
                                <TextBlock Text="Last backup" Style="{StaticResource Text.Body}" Margin="0,3" />
                                <TextBlock Text="Last backup failure" Style="{StaticResource Text.Body}" Margin="0,3" />
                                <TextBlock Text="Integrity" Style="{StaticResource Text.Body}" Margin="0,3" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding AppVersion}" Foreground="{DynamicResource Brush.Text}" Margin="0,3" />
                                <TextBlock Text="{Binding SchemaVersion}" Foreground="{DynamicResource Brush.Text}" Margin="0,3" />
                                <TextBlock Text="{Binding DatabaseLocation}" Foreground="{DynamicResource Brush.TextMuted}" Margin="0,3" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding DatabaseSize}" Foreground="{DynamicResource Brush.Text}" Margin="0,3" />
                                <TextBlock Text="{Binding AvailableDisk}" Foreground="{DynamicResource Brush.Text}" Margin="0,3" />
                                <TextBlock Text="{Binding LastBackup}" Foreground="{DynamicResource Brush.Text}" Margin="0,3" />
                                <TextBlock Text="{Binding LastBackupFailure}" Foreground="{DynamicResource Brush.Text}" Margin="0,3" />
                                <TextBlock Text="{Binding IntegrityStatus}" Foreground="{DynamicResource Brush.Accent}" FontWeight="SemiBold" Margin="0,3" />
                            </StackPanel>
                        </Grid>
                        <WrapPanel Margin="0,14,0,0" Visibility="{Binding CanRun, Converter={StaticResource BoolToVisible}}">
                            <Button Content="Run database check" Command="{Binding RunCheckCommand}" Style="{StaticResource Button.Primary}" Margin="0,0,8,8" />
                            <Button Content="Validate folders" Command="{Binding ValidateFoldersCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,8,8" />
                            <Button Content="Create diagnostic summary" Command="{Binding CreateDiagnosticCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,8,8" />
                            <Button Content="Create support bundle" Command="{Binding CreateSupportBundleCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,8,8" />
                            <Button Content="Open logs folder" Command="{Binding OpenLogsCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,8,8" />
                            <Button Content="Open backups folder" Command="{Binding OpenBackupsCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,0,8" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Managed folders" Foreground="{DynamicResource Brush.Text}" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding Folders}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1" Padding="0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="120" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="{DynamicResource Brush.Text}" FontWeight="SemiBold" />
                                            <TextBlock Grid.Column="1" Text="{Binding State}" Foreground="{DynamicResource Brush.TextMuted}" />
                                            <TextBlock Grid.Column="2" Text="{Binding Detail}" Foreground="{DynamicResource Brush.TextMuted}" />
                                            <TextBlock Grid.Column="3" Text="{Binding Path}" Foreground="{DynamicResource Brush.TextMuted}" TextWrapping="Wrap" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
