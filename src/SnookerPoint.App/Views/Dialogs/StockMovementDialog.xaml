<Window x:Class="SnookerPoint.App.Views.Dialogs.StockMovementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stock movement"
        FontFamily="{StaticResource Font.Ui}"
        Width="440" SizeToContent="Height"
        ResizeMode="NoResize" WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background}">
    <Border Background="{DynamicResource Brush.Surface}" Padding="24">
        <StackPanel>
            <TextBlock Text="{Binding ProductName}" Style="{StaticResource Text.Heading}" />
            <TextBlock Style="{StaticResource Text.Caption}" Margin="0,4,0,16">
                <Run Text="Current stock: " />
                <Run Text="{Binding CurrentStockText, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>

            <TextBlock Text="Movement type" Style="{StaticResource Text.Caption}" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding MovementTypes}" SelectedItem="{Binding SelectedType}" Padding="8,7" />

            <TextBlock Text="Quantity" Style="{StaticResource Text.Caption}" Margin="0,12,0,4" />
            <TextBox x:Name="QtyBox" Text="{Binding QuantityText, UpdateSourceTrigger=PropertyChanged}" Padding="8,7" />

            <TextBlock Text="Reason / reference" Style="{StaticResource Text.Caption}" Margin="0,12,0,4" />
            <TextBox Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}" Padding="8,7" />

            <Border Background="{DynamicResource Brush.Card}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
                    CornerRadius="8" Padding="12,10" Margin="0,14,0,0">
                <TextBlock Foreground="{DynamicResource Brush.Text}">
                    <Run Text="After this change: " />
                    <Run Text="{Binding PreviewText, Mode=OneWay}" FontWeight="SemiBold" />
                </TextBlock>
            </Border>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource Brush.Danger}" TextWrapping="Wrap" Margin="0,12,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsed}}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                <Button Content="Cancel" IsCancel="True" Style="{StaticResource Button.Secondary}" Margin="0,0,10,0" />
                <Button Content="Confirm" Click="OnConfirm" IsDefault="True" Style="{StaticResource Button.Primary}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
